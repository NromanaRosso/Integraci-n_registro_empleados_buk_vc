import { SyncEmployeesUseCase } from './sync-employees.use-case';

describe('SyncEmployeesUseCase', () => {
  it('should be defined', () => {
    expect(new SyncEmployeesUseCase()).toBeDefined();
  });
});
