import { SyncSingleEmployeeUseCase } from './sync-single-employee.use-case';

describe('SyncSingleEmployeeUseCase', () => {
  it('should be defined', () => {
    expect(new SyncSingleEmployeeUseCase()).toBeDefined();
  });
});
